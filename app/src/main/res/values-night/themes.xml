<resources>
    <!-- Base application theme. -->
    <style name="Theme.SecuritiesExam" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/btn_confirm</item>

        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">false</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomSideSheetDialog" parent="Theme.Material3.Light.SideSheetDialog">
        <item name="backgroundTint">@color/dialog_back</item>
    </style>
</resources>